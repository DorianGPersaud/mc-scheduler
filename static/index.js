/*var classes = 1;

var addButton = $('#add-class');

addButton.click(function(e) {
    var classList = $('#class-list input');
    var lastClass = classList[classList.length - 2];
    var lastName = lastClass.name;
    var newNumber = parseInt(lastName[lastName.length - 1]) + 1;

    $(lastClass).after('<input type="text" name="class' + newNumber + '">');
});*/

/*var classes = $('#classes');
var submitButton = $('#submit');
var form = $('#submit-form');

submitButton.click(function(e) {
    var classList = classes.val().split('\n').join('-');
    console.log(classList);

    window.location.href = 'http://www.google.com/'
});*/